name: Workflow iniziale

"on": [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Run script
        run: echo "Ciao, dalla GitHub Action"
